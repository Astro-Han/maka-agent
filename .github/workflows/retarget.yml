name: CI retarget probe
on:
  pull_request:
    branches: [test/ci-gate-base]
    types: [edited]
permissions:
  contents: read
jobs:
  test:
    if: github.event.changes.base.ref.from != ''
    uses: ./.github/workflows/validation.yml
